trigger:
  branches:
    include:
      - develop

variables:
  - group: tpfinal-vars

stages:
  - stage: Build
    jobs:
      - job: BuildFront
        pool:
          vmImage: ubuntu-latest
        steps:
          - template: templates/_build-front.yml